{
  "description": "BIOS Implant session-start boot instruction, plus a read-only notice when this machine still uses the retired 8hats/plugins marketplace. Neither hook performs network access or writes state.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/boot-protocol.mjs\"",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/old-marketplace-notice.mjs\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
